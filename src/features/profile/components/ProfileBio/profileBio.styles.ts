import { Typography } from '@mui/material';
import { styled } from '@mui/material/styles';

/** Centers the text inside the bio section. */
export const StyledTypography = styled(Typography)({
    width: '100%',
    height: '100%',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
});
