import { CircularProgress } from '@mui/material';

/**
 * Added reusable loader component for the application
 */
const Loader = () => <CircularProgress color="inherit" size={20} />;

export default Loader;
